# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Logs
*.log

# Node (ileride eklenirse)
node_modules/
dist/

# Claude Code (kişisel ayarlar commit'lenmez)
.claude/settings.local.json

# Ham kaynak ses dosyaları (sadece AUDIO/ klasörü commit'lenir)
RESOURCES/
*.zip

# Tasarım kaynak dosyaları (PNG/ klasöründeki export'lar yeterli)
*.pdf

# Eski prototip
arcie.html

# (WAV master RESOURCES/ArcRiseBGMusic_master.wav'a taşındı — RESOURCES/ zaten ignore'da.
#  AUDIO/ artık yalnızca oyunun kullandığı dosyaları içerir ve olduğu gibi paketlenir.)

# İmzalama anahtarı — ASLA commit'lenmez (Play Store upload key + şifreler)
keystore/
*.jks
*.keystore
keystore_credentials.txt
android/keystore.properties

# www/ üretilmiş kopyadır (kaynak = root dosyalar; "npm run sync" ile üretilir)
www/

# Android build çıktıları
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/app/release/
*.aab
*.apk
